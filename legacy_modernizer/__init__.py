"""
Legacy Modernizer — AI-powered legacy code analysis and modernization engine.

This package provides modules for:
  - Ingesting legacy repositories (COBOL, Java, etc.)
  - Analyzing dependencies between functions/modules
  - Filtering dead code and noise
  - Building optimized context via Scaledown
  - Generating modern Python/Go equivalents via OpenRouter LLMs

Treat `scaledown` as an internal library dependency — never modify its core.
"""

__version__ = "0.1.0"
